@attribute [Authorize(Policy = VctoonPermissions.Library.Create)]

<CreateEditLibraryBase OnCreatedOrUpdated="OnSucceeded"></CreateEditLibraryBase>

@code {

    [Parameter] public EventCallback OnSucceeded { get; set; }
}