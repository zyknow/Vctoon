@attribute [Authorize(Policy = VctoonPermissions.Library.Update)]

<CreateEditLibraryBase LibraryId="@LibraryId" OnCreatedOrUpdated="OnSucceeded"></CreateEditLibraryBase>

@code {
    [Parameter] public virtual Guid LibraryId { get; set; }

    [Parameter] public EventCallback OnSucceeded { get; set; }
}